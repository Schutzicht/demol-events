---
import { Image } from 'astro:assets';

const content = {
  label: "Ervaren Eventmanagement",
  heading: {
    line1: "JOUW EVENT,",
    highlight: "PERFECT GEREGELD."
  },
  description: "Geen stress, geen verrassingen. Wij nemen de volledige organisatie uit handen. Of het nu gaat om een bedrijfsfeest of congres: wij zorgen voor impact. Al meer dan 20 jaar.",
  bgImage: "/Concert-at-SEA-Persfoto-2-scaled-aspect-ratio-800-450-scaled.jpg"
};
---

<section class="relative min-h-[100dvh] w-full flex items-center overflow-hidden bg-gray-50/50">
  
  {/* Blurry Background Layer */}
  <div class="absolute inset-0 z-0">
    <div class="absolute inset-0 bg-white/60 z-10"></div>
    <img 
      src={content.bgImage}
      alt="" 
      class="w-full h-full object-cover blur-[100px] scale-125 opacity-50"
    />
  </div>
  
  <div class="container mx-auto px-6 h-full relative z-10">
    <div class="flex flex-col lg:flex-row items-center gap-12 lg:gap-20 h-full pt-20 lg:pt-0">
      
      {/* Left Content - Clean & Stong */}
      <div class="w-full lg:w-1/2 flex flex-col justify-center space-y-8 z-10 order-2 lg:order-1 pb-20 lg:pb-0">
        
        <div class="flex items-center gap-3 animate-slide-up opacity-0 fill-mode-forwards mb-2" style="animation-delay: 0.1s;">
          <span class="w-8 md:w-12 h-[2px] bg-dme-teal"></span>
          <span class="text-dme-teal font-bold tracking-widest uppercase text-xs md:text-sm">
            {content.label}
          </span>
        </div>

        <h1 class="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-heading font-black tracking-tight leading-[1.1] text-gray-900 animate-slide-up opacity-0 fill-mode-forwards" style="animation-delay: 0.2s;">
          JOUW EVENT IN <span class="text-dme-teal">ZEELAND</span>,<br/>
          PERFECT GEREGELD.
        </h1>

        <p class="text-xl text-gray-600 max-w-lg leading-relaxed font-medium animate-slide-up opacity-0 fill-mode-forwards" style="animation-delay: 0.3s;">
          Dé <strong>eventmanager in Zeeland</strong> die alles uit handen neemt. Van bedrijfsfeest tot congres: wij zorgen voor impact en een vlekkeloze organisatie.
        </p>

        <div class="flex flex-wrap gap-4 pt-4 animate-slide-up opacity-0 fill-mode-forwards" style="animation-delay: 0.4s;">
            <a href="#contact" class="group relative px-8 py-4 bg-dme-accent text-white font-bold text-lg rounded-full overflow-hidden transition-all hover:scale-105 active:scale-95 shadow-xl shadow-dme-accent/20 hover-shine">
              <span class="relative z-10 flex items-center gap-2">
                Start project
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="group-hover:translate-x-1 transition-transform"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg>
              </span>
              <div class="absolute inset-0 bg-dme-accent-dark transform scale-x-0 group-hover:scale-x-100 transition-transform duration-300 origin-left"></div>
            </a>
            
            <a href="#portfolio" class="px-8 py-4 rounded-full border-2 border-gray-200 text-gray-900 font-bold hover:border-dme-teal hover:text-dme-teal transition-all bg-transparent">
              Bekijk werk
           </a>
        </div>



      </div>

      {/* Right Image - Dynamic Layout */}
      <div class="w-full lg:w-1/2 relative h-[50vh] lg:h-auto lg:aspect-square flex items-center justify-center order-1 lg:order-2">
         
         <div class="absolute inset-0 bg-dme-teal/5 rounded-[3rem] transform rotate-6 scale-90 -z-10"></div>
         
         <div class="relative w-full h-full max-h-[80vh] rounded-[2rem] overflow-hidden shadow-2xl animate-scale-in">
            <img 
              src={content.bgImage}
              alt="DeMol Events Atmosphere" 
              class="w-full h-full object-cover"
            />
            
            {/* Floating Badge */}
            <div class="absolute bottom-8 left-8 bg-white/90 backdrop-blur-md p-6 rounded-2xl shadow-lg max-w-xs animate-float-slow hidden md:block">
               <div class="flex items-center gap-3 mb-2">
                  <div class="flex -space-x-2">
                    <div class="w-6 h-6 rounded-full bg-yellow-400 border-2 border-white flex items-center justify-center text-[8px] font-bold text-yellow-900">★</div>
                    <div class="w-6 h-6 rounded-full bg-yellow-400 border-2 border-white flex items-center justify-center text-[8px] font-bold text-yellow-900">★</div>
                    <div class="w-6 h-6 rounded-full bg-yellow-400 border-2 border-white flex items-center justify-center text-[8px] font-bold text-yellow-900">★</div>
                  </div>
                  <span class="text-xs font-bold uppercase tracking-wider text-gray-500">Bewezen kwaliteit</span>
               </div>
               <p class="font-heading font-bold text-lg text-gray-900">Al 20+ jaar <br/>de expert in Events.</p>
            </div>
         </div>

      </div>

    </div>
  </div>
</section>

<style>
  .perspective-1000 {
    perspective: 1000px;
  }
  
  @keyframes slide-up {
    from {
      opacity: 0;
      transform: translateY(40px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes subtle-zoom {
    0% { transform: scale(1.1); }
    50% { transform: scale(1.15); }
    100% { transform: scale(1.1); }
  }

  @keyframes float-slow {
    0%, 100% { transform: translateY(0) rotate(0deg); }
    50% { transform: translateY(-20px) rotate(2deg); }
  }

  @keyframes float-medium {
    0%, 100% { transform: translateY(0) rotate(0deg); }
    50% { transform: translateY(-15px) rotate(-2deg); }
  }

  .animate-slide-up {
    animation: slide-up 1s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .animate-subtle-zoom {
    animation: subtle-zoom 20s ease-in-out infinite;
  }

  .animate-float-slow {
    animation: float-slow 8s ease-in-out infinite;
  }

  .animate-float-medium {
    animation: float-medium 6s ease-in-out infinite;
  }
  
  .fill-mode-forwards {
    animation-fill-mode: forwards;
  }
</style>
