---
import { Image } from 'astro:assets';

const content = {
  label: "Event Productie",
  heading: {
    line1: "DEMOL",
    highlight: "EVENTS."
  },
  description: "We creëren, produceren en realiseren evenementen die bijblijven. Van concept tot uitvoering, wij regelen het.",
  bgImage: "https://lirp.cdn-website.com/3a60022f/dms3rep/multi/opt/Schermafbeelding+2018-08-05+om+14.11.25-2880w.jpg"
};
---

<section class="relative min-h-[100dvh] w-full overflow-hidden flex flex-col justify-center items-center perspective-1000">
  
  {/* Cinematic Background with Parallax */}
  <div class="absolute inset-0 z-0 select-none pointer-events-none">
     <div class="absolute inset-0 bg-dme-black/30 z-10 mix-blend-multiply"></div>
     <div class="absolute inset-x-0 bottom-0 h-2/3 bg-gradient-to-t from-dme-black via-dme-black/80 to-transparent z-20"></div>
     
     <img 
      src={content.bgImage}
      alt="DeMol Events Atmosphere" 
      class="w-full h-full object-cover scale-110 animate-subtle-zoom will-change-transform"
      style="transform-origin: center center;"
    />
  </div>

  <div class="container relative z-30 px-6 mx-auto h-full flex flex-col justify-center items-center text-center">
    
    {/* Floating Elements */}
    <div class="absolute top-1/4 left-10 hidden lg:block animate-float-slow opacity-60 mix-blend-overlay">
        <div class="w-32 h-32 rounded-full border border-white/20 backdrop-blur-md"></div>
    </div>
    <div class="absolute bottom-1/4 right-10 hidden lg:block animate-float-medium opacity-60 mix-blend-overlay">
        <div class="w-48 h-48 rounded-full border border-dme-teal/20 backdrop-blur-md bg-dme-teal/5"></div>
    </div>

    {/* Main Content */}
    <div class="relative max-w-5xl mx-auto space-y-8">
      
      {/* Label */}
      <div class="overflow-hidden mb-4">
        <span class="inline-block bg-white/90 backdrop-blur-sm px-4 py-1 rounded-full text-dme-teal font-mono uppercase tracking-[0.2em] text-xs font-bold animate-slide-up opacity-0 fill-mode-forwards shadow-lg" style="animation-delay: 0.2s;">
          {content.label}
        </span>
      </div>

      {/* Headline - Big & Bold */}
        <h1 class="text-5xl md:text-7xl lg:text-8xl font-heading font-black tracking-tight mb-8 leading-[1.1]">
          <span class="block text-gray-900" style="animation-delay: 0.3s;">
            {content.heading.line1}
          </span>
          <span class="block text-transparent bg-clip-text bg-gradient-to-r from-dme-teal to-dme-teal-dark pb-4" style="animation-delay: 0.4s;">
            {content.heading.highlight}
          </span>
        </h1>

        <div class="text-sm text-gray-500">
          <span class="text-gray-900 font-bold block">Vertrouwd door</span>
          vele tevreden klanten.
        </div>

      {/* Actions */}
      <div class="flex flex-col sm:flex-row items-center justify-center gap-6" style="animation-delay: 0.6s;">
          <a href="#contact" class="group relative px-8 py-4 bg-dme-teal text-white font-bold text-lg rounded-full overflow-hidden transition-all hover:scale-105 active:scale-95 shadow-lg shadow-dme-teal/30">
            <span class="relative z-10">Start jouw project</span>
            <div class="absolute inset-0 bg-dme-teal-dark transform scale-x-0 group-hover:scale-x-100 transition-transform duration-300 origin-left"></div>
          </a>
          
          <a href="#portfolio" class="group flex items-center gap-3 px-6 py-4 rounded-full hover:bg-gray-100 transition-all text-gray-600 hover:text-dme-teal font-medium">
            <span>of bekijk ons werk</span>
            <div class="w-8 h-8 rounded-full bg-gray-100 flex items-center justify-center group-hover:bg-dme-teal group-hover:text-white transition-all">
             <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="transform group-hover:translate-x-0.5 transition-transform"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
           </div>
         </a>
      </div>

    </div>
  </div>

  {/* Scroll Indicator */}
  <div class="absolute bottom-10 left-1/2 transform -translate-x-1/2 animate-bounce-slow opacity-50 mix-blend-screen">
    <div class="w-[1px] h-16 bg-gradient-to-b from-transparent via-white to-transparent"></div>
  </div>

</section>

<style>
  .perspective-1000 {
    perspective: 1000px;
  }
  
  @keyframes slide-up {
    from {
      opacity: 0;
      transform: translateY(40px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes subtle-zoom {
    0% { transform: scale(1.1); }
    50% { transform: scale(1.15); }
    100% { transform: scale(1.1); }
  }

  @keyframes float-slow {
    0%, 100% { transform: translateY(0) rotate(0deg); }
    50% { transform: translateY(-20px) rotate(2deg); }
  }

  @keyframes float-medium {
    0%, 100% { transform: translateY(0) rotate(0deg); }
    50% { transform: translateY(-15px) rotate(-2deg); }
  }

  .animate-slide-up {
    animation: slide-up 1s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .animate-subtle-zoom {
    animation: subtle-zoom 20s ease-in-out infinite;
  }

  .animate-float-slow {
    animation: float-slow 8s ease-in-out infinite;
  }

  .animate-float-medium {
    animation: float-medium 6s ease-in-out infinite;
  }
  
  .fill-mode-forwards {
    animation-fill-mode: forwards;
  }
</style>
